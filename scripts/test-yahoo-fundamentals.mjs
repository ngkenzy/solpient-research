import assert from "node:assert/strict";import{parseYahooTimeseries,normalizeYahooFundamentals,YAHOO_FUNDAMENTALS_PROVIDER}from"../lib/yahoo-fundamentals.mjs";
function result(prefix,key,values){return{[prefix+key]:values.map(([date,raw])=>({asOfDate:date,reportedValue:{raw}}))};}const q=["2026-06-30","2026-03-31","2025-12-31","2025-09-30","2025-06-30"],a=["2026-06-30","2025-06-30","2024-06-30","2023-06-30"],keys={TotalRevenue:100,NetIncome:20,OperatingCashFlow:30,FreeCashFlow:25,DilutedAverageShares:10,DilutedEPS:2,GrossProfit:70,OperatingIncome:40,CashAndCashEquivalents:50,CurrentAssets:120,CurrentLiabilities:70,StockholdersEquity:200,RetainedEarnings:100,TotalAssets:500,TotalLiabilitiesNetMinorityInterest:300,TotalDebt:40,StockBasedCompensation:3},body=(prefix,dates)=>({timeseries:{result:Object.entries(keys).map(([k,v])=>result(prefix,k,dates.map((d,i)=>[d,v+i]))) }});assert.equal(parseYahooTimeseries(body("quarterly",q),"quarterly").length,5);const x=normalizeYahooFundamentals({company:{id:"c1",ticker:"MSFT"},quarterlyBody:body("quarterly",q),annualBody:body("annual",a),industryModule:"software_platform",observedAt:"2026-09-20T00:00:00Z"});assert.equal(x.quality.passed,true);assert.equal(x.quarterly.length,5);assert.equal(x.annual.length,4);assert.equal(x.quarterly[0].provider,YAHOO_FUNDAMENTALS_PROVIDER);assert.equal(x.annual[0].fiscal_period,"FY");assert.equal(x.quarterly[0].raw_payload.balance_sheet.totalDebt,40);console.log("Yahoo fundamentals fallback tests passed.");